document.querySelectorAll(".filters").forEach(group=>{const items=group.nextElementSibling.querySelectorAll("[data-tags]");group.querySelectorAll("button").forEach(btn=>btn.onclick=()=>{group.querySelectorAll("button").forEach(b=>b.classList.remove("active"));btn.classList.add("active");const f=btn.dataset.filter;items.forEach(i=>i.classList.toggle("hidden",f!=="all"&&!i.dataset.tags.split(" ").includes(f)))})});